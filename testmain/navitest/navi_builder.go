package navi

import (
	//"reflect"
	//"git.oschina.net/kuaishangtong/navi/testmain/navi"
)

func NaviServiceClient_Methods() {
	/*serviceClient := new(navi.NaviServiceClient)
	numMethod := reflect.TypeOf(serviceClient).Elem().NumMethod()

	for i := 0; i < numMethod; i++ {
		funcName := reflect.TypeOf(serviceClient).Elem().Method(i).Name
		params := reflect.TypeOf(serviceClient).Elem().Method(i).Type
		//fmt.Printf("name:%s, params:%s \n",funcName,params)

		if funcName == "TestFunc" {
		//	//fv := reflect.ValueOf(TestFunc)
			fv := reflect.ValueOf(serviceClient).Elem().MethodByName(funcName)
		//	params := make([]reflect.Value,1)
		//	params[0] = reflect.ValueOf(100)
		//	rs := fv.Call(params)
		//	fmt.Println("result:",fv)
		}
	}*/
}